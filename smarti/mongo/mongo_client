"""mongo db access"""

import pymongo


client = pymongo.MongoClient("localhost", 27017)


def read_workbooks(workbook_id: str) -> list:
    """read workbook from database"""
    return [client.smarti.name, workbook_id]

